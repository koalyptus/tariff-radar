{
  "name": "tariff-radar",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "corepack pnpm --filter @tariff-radar/registry build",
    "generate-registry": "corepack pnpm --filter @tariff-radar/registry generate-registry",
    "typecheck": "corepack pnpm --filter @tariff-radar/registry typecheck"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
