{
  "name": "tariff-radar",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "generate-registry": "pnpm --filter @tariff-radar/registry generate-registry",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
